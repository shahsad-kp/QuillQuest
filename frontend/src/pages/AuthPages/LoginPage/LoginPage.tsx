import '../AuthPages.scss'
import {LoginForm} from "../../../components";

export const LoginPage= () => {
    return (
        <div className={'auth-page'}>
            <LoginForm/>
        </div>
    );
};